<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="../static/main.css" media="screen">
    <title>Giriş Yap</title>
  </head>
  <body>
 
 
    <div class="container main">
        <h1>Flask-Project</h1>
        <h3>Giriş Yap</h3>
        <form method="post" action="">
          <input type="text" name="username" placeholder="Kullanıcı Adı">
          <input type="password" name="password" placeholder="Şifre">
          <button type="submit" name="button">Giriş Yap</button>
   </form>
      <div class="container">
        <div class="row">
          <div class="col-xs-5">
            {% if error %}
              <p class="bg-danger text-center hiza">{{error}}</p>
            {% endif %}
          </div>
	<div class="col-xs-5">
            {% for m in get_flashed_messages() %}
              <p class="bg-success text-center hiza">{{m}}</p>
            {% endfor %}
          </div>
 
        </div>
      </div>
    </div>
 
 
  </body>
</html>
